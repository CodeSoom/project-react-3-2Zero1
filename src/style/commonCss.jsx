import styled from '@emotion/styled';

export const Button = styled.button({
  fontSize: '12px',
  margin: '10px',
  padding: '5px 8px',
  border: 'none',
  borderRadius: '15px',
});

export const Information = styled.p(() => ({
  margin: '5px 15px',
  fontSize: '12px',
  color: 'gray',
  textAlign: 'center',
}));
