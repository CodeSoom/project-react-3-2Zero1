import styled from '@emotion/styled';

const Button = styled.button({
  fontSize: '12px',
  margin: '10px',
  padding: '5px 8px',
  border: 'none',
  borderRadius: '15px',
  color: '#585858',
});

export default Button;
